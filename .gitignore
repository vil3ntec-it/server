node_modules/
# فقط پوشهٔ دادهٔ ریشه — نه هر پوشه‌ای که اسمش data باشد.
# قبلاً این قاعده بدونِ اسلشِ ابتدایی بود و بستهٔ کاتلینِ data/ را هم
# بی‌صدا از گیت بیرون می‌گذاشت: فایل‌ها روی دیسک بودند ولی در CI نبودند.
/data/
.env
*.log
dist/

# خروجی و کشِ ساختِ اندروید — جای اینها در مخزن نیست
.gradle/
homelab-panel/admin-android/build/
homelab-panel/admin-android/app/build/

# سرورِ حساب (shop/server) که CI کنارِ پنل می‌گذارد — کدش در ریپوی shop است
homelab-panel/account-server/
