# نقشهٔ راهِ «سرور خانگیِ حرفه‌ای»

این فایل، فهرستِ ۵۹ بخشیِ خواسته‌شده را به کارهای واقعی می‌شکند و می‌گوید
هر کدام در چه وضعی است. هر بار که چیزی تمام شود، همین‌جا به‌روز می‌شود.

وضعیت‌ها:  ✅ انجام شده   ·   🔨 در دست   ·   ⬜ نوبتش نرسیده

---

## مرحلهٔ ۱ — کتابخانه و نظمِ فایل‌ها  (نسخهٔ 1.3.0)

| بخش | کار | وضعیت |
|-----|-----|-------|
| ۶ | انتخابِ محلِ ذخیره‌سازی و ساختِ ساختارِ مرکزی | ✅ |
| ۷ | پوشهٔ مستقل برای هر سایت و هر برنامه، با جداسازیِ نامِ تکراری | ✅ |
| ۸ | بخشِ Library در برنامه (فهرست، اندازه، مسیر، بازکردنِ پوشه) | ✅ |
| ۹ | ساختِ خودکارِ پوشهٔ سایت (app/data/logs/backup/config) | ✅ |
| ۱۰ | ساختِ خودکارِ پوشهٔ برنامه (data/logs/config/backup) | ✅ |
| ۱۱ | جای دیتابیس: `Server/database` و `Project/data` | ✅ ساختار آماده · 🔨 انتقالِ دیتابیسِ فعلی |
| ۱۲ | جای لاگ‌ها: `Server/logs` و `Sites/…/logs` | ✅ ساختار آماده · 🔨 هدایتِ لاگِ فعلی |
| ۱۳ | جای پشتیبان‌ها: `Backups/{Daily,Weekly,Manual}` | ✅ ساختار آماده |
| ۱۴ | فایل‌های موقت فقط در `Temp/` + نظافتِ خودکار | ✅ |
| ۱۵ | پیدا کردنِ پوشه‌های پراکنده، پیش‌نمایش، انتقالِ امن، `Unsorted/` | ✅ |
| ۱۶ | Settings → Storage با فضای آزاد/پر/کل | ✅ |
| ۱۷ | تغییرِ محلِ ذخیره‌سازی با بررسیِ فضا و اجازه | ✅ بررسی و ساخت · ⬜ انتقالِ کاملِ دادهٔ قدیمی |
| ۱۸ | خطای واضح وقتی درایو در دسترس نیست | ✅ |
| ۱۹ | هشدارِ کمبودِ فضا (هشدار/بحرانی) | ✅ |

## آن‌چه پیش‌تر تمام شده

| بخش | کار | وضعیت |
|-----|-----|-------|
| ۲۵ | نصب‌کنندهٔ حرفه‌ای (خوش‌آمد ← پوشه ← نصب ← پایان) | ✅ |
| ۲۷ | نصبِ خودکارِ وابستگی‌ها با نمایشِ خروجی | ✅ |
| ۲۹ | میان‌برِ دسکتاپ با آیکونِ واقعی + خودترمیم | ✅ |
| ۳۰ | منوی استارت | ✅ |
| ۴۲ | Connected Apps (برنامه‌ها و سایت‌ها با کلید و آدرسِ جدا) | ✅ |
| ۴۵ | OTP شش‌رقمی: هش، انقضا، سقفِ تلاش، سقفِ ساعتی، فاصلهٔ ارسال | ✅ |
| ۴۶ | پیامک با پنج سرویس + webhook؛ بدونِ «موفقیتِ دروغین» | ✅ |
| ۴۷ | ایمیل SMTP/TLS با تستِ واقعی | ✅ |
| ۵۵ | خطاهای ساده برای کاربر + گزارشِ فنی در فایل | ✅ |
| ۵۸ | به‌روزرسانی با پشتیبان‌گیری و بازگشت | ✅ |

## مرحلهٔ ۲ — امنیت و پایداری  (بعدی)

| بخش | کار | وضعیت |
|-----|-----|-------|
| ۲۳ | سفت‌کردنِ File Manager برابرِ Path Traversal و Symlink و Junction | 🔨 |
| ۴۱ | حذفِ `APP_BUILTIN_TOKEN` ثابت و جایگزینی با رمزِ هر نصب | 🔨 |
| ۴۳ | محدودیتِ نرخ روی همهٔ APIها + CORS محدود (الان `*` است) | ⬜ |
| ۴۴ | توکنِ کوتاه‌عمر و heartbeat برای WebSocket | ⬜ |
| ۴۰ | HTTPS/TLS محلی و wss | ⬜ |
| ۵۲ | مهاجرت و نسخه‌بندیِ دیتابیس | ⬜ |

## مرحلهٔ ۳ — سرویس و کشفِ خودکار

| بخش | کار | وضعیت |
|-----|-----|-------|
| ۳۵ | اجرای خودکار با بالا آمدنِ ویندوز، جدا از پنجرهٔ برنامه | ⬜ |
| ۳۶ | Windows Service با Start/Stop/Restart | ⬜ |
| ۳۸ | پیدا شدنِ خودکارِ سرور با mDNS و UDP | ⬜ |
| ۳۳ | Uninstaller با گزینهٔ نگه‌داشتنِ داده | ⬜ |
| ۳۴ | Repair Installation | ⬜ |
| ۲۶ | نصبِ خودکارِ Node.js | ⬜ |

## مرحلهٔ ۴ — رابط و تجربهٔ کاربری

| بخش | کار | وضعیت |
|-----|-----|-------|
| ۴ | Simple Mode / Advanced Mode | ⬜ |
| ۵ | داشبوردِ کاملِ CPU/RAM/Disk/Network در برنامه | ⬜ |
| ۲۱ | صفحهٔ مدیریتِ هر سایت (Overview/Files/Logs/Settings/Backup) | ⬜ |
| ۲۲ | File Manager داخلِ برنامه | ⬜ |
| ۴۹ | پشتیبان‌گیریِ زمان‌بندی‌شده با نگه‌داری و بازیابی | ⬜ |
| ۵۶ | جست‌وجوی سراسری | ⬜ |

---

## چیزی که در بازرسی پیدا شد

* هیچ `TODO` / `FIXME` / دادهٔ ساختگی در کدِ سرور نیست.
* تنها رمزِ ثابتِ کد: `APP_BUILTIN_TOKEN` در `sitesync/index.js` — عمداً آن‌جاست
  (در `index.html` عمومیِ سایت هم هست) ولی باید جایگزین شود. مرحلهٔ ۲.
* `Access-Control-Allow-Origin` روی `*` است. برای شبکهٔ خانگی درست کار می‌کند
  ولی باید محدود شود. مرحلهٔ ۲.
* `routes/messenger.js` محافظِ احراز هویت ندارد — عمدی است (پیام‌رسانِ عمومیِ
  سایت)، ولی باید محدودیتِ نرخ بگیرد. مرحلهٔ ۲.
